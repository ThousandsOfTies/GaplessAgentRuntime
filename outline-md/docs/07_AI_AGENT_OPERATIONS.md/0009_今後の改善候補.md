## 今後の改善候補

- OLED framebuffer の期待値チェックを追加する
- `/api/events` で操作履歴を取得する
- 実機 RasPi5 にも同じ `run / logs / diagnose` 抽象を用意する
- `gar sim run <target>` / `gar target run <target>` を共通 manifest 化する
- `gar sim run <target>` / `gar target run <target>` を共通 manifest 化し、`~/sensor_demo` 起動後の検証ログ収集まで `gar` に畳む
- systemd 化して EC2 上の起動/停止を安定させる

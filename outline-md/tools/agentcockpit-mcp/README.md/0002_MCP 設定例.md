## MCP 設定例

```json
{
  "mcpServers": {
    "gar": {
      "command": "python3",
      "args": ["/home/user/Yurufuwa/AgentCockpit/tools/agentcockpit-mcp/server.py"]
    }
  }
}
```

現在の環境向けの設定は `make init` で生成できます。

```bash
.gar/mcp-config.json
```

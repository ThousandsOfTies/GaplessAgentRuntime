"""Artifact-domain logic shared across `gar` command scopes.

This package does not correspond to any single `gar` CLI argument (unlike
:mod:`scripts.gar_lib.application`, it is domain/implementation
logic called *by* the command dispatch modules under
:mod:`scripts.gar_lib.commands`).
"""

"""Drop-in development environment providers.

Each Python module in this package may define one DevEnvironment subclass.
agp init discovers these modules directly, so there is no separate provider
list to maintain.
"""
